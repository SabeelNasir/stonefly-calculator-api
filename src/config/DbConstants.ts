export class DbConstants {
    static LIST: any = {
        quotationsList: ["quotation.companyId", "quotation.quotationId", "quotation.totalDiscount", "quotation.totalDiscountedPrice", "quotation.totalPrice", "quotation.quoteNo", "quotation.title", "quotation.company", "quotationDetail.productId", "quotationDetail.quantity", "quotationDetail.quotationDetailId"],
        companiesList: ["companyId", "name", "address", "contactName", "contactNo", "email"]
    }
}